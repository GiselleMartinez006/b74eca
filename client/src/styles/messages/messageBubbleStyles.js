import { makeStyles } from "@material-ui/core/styles";

export  const useStyles = makeStyles(() => ({
  
    img: {
      maxWidth: "150px",
      height: "auto",
      width: "auto",
      maxHeight: "150px",
    },
    grouppedImgs: {
      margin: "5px",
      maxWidth: "150px",
      height: "125px",
      borderRadius: "5px",
    },
  }));